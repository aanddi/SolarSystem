<% layout('./layout/page.ejs') %>
   <div class="main-screen">
      <div class="main-screen__user">
         <% if(user.length > 0) {%>
            <h1>Привет <%= user[0].username %>
            </h1>
            <% } else { %>
               <h1>Зарегистрируйтесь, чтобы видеть все страницы сайта</h1>
               <% } %>
      </div>
      <div class="main-screen__counter">
         Счетчик: <%= counter %>
      </div>
   </div>